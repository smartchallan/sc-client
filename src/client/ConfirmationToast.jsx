import React from 'react';

// Optional mounting point for apps that prefer a React component to own toasts.
// The notifications helper already creates a DOM container; this component is a no-op
// placeholder that can be extended to render toasts via React if desired.
export default function ConfirmationToast() {
  return null;
}
